---
phase: 03.2-rsvp-automation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [pocketbase/pb_hooks/auto_status.pb.js, pocketbase/pb_hooks/token_service.pb.js, pocketbase/pb_hooks/quick_rsvp.pb.js]
autonomous: true
requirements: [RSVP-03, RSVP-06, RSVP-07]
must_haves:
  truths:
    - "Status automation correctly handles precedence (Upcoming Yes > Past Missed)"
    - "Status automation considers both RSVP and Attendance for past events"
    - "RSVP tokens are generated on the server and verified via HMAC"
  artifacts:
    - path: "pocketbase/pb_hooks/auto_status.pb.js"
      provides: "Status recalculation engine"
    - path: "pocketbase/pb_hooks/token_service.pb.js"
      provides: "Secure token generation endpoint"
    - path: "pocketbase/pb_hooks/quick_rsvp.pb.js"
      provides: "Public RSVP processing"
---

<objective>
Implement the server-side logic for status automation and secure token-based RSVPs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-3/SPEC.md
@pocketbase/pb_hooks/utils.pb.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Status Automation Engine</name>
  <files>pocketbase/pb_hooks/auto_status.pb.js</files>
  <action>
    Create a hook on `eventRosters` (onModelAfterUpdate, onModelAfterCreate):
    1. Fetch Profile. If `statusIsManual === true`, exit.
    2. Check for "Active (Current)": Any "Yes" RSVP for an upcoming event where `isOpenForRSVP === true` and `type === 'Performance'`.
    3. Check for "Inactive": Last 3 past performances (where `event.date > profile.created`) were "Missed".
       - Missed = (`rsvp` in [No, Pending]) AND (`attendance` in [Absent, Pending]).
    4. Fallback to "Active (Future)".
    5. Optimization: Only update profile if status/reason changed. Use Transaction.
  </action>
  <verify>
    <automated>ls pocketbase/pb_hooks/auto_status.pb.js</automated>
  </verify>
  <done>Status automation logic implemented with correct precedence and attendance awareness.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Server-Side Token Generation API</name>
  <files>pocketbase/pb_hooks/token_service.pb.js</files>
  <action>
    Create `POST /api/generate-rsvp-tokens`:
    1. Restricted to Admin.
    2. Input: `eventId`, `profileIds[]`.
    3. Retrieve `HMAC_SECRET` via `utils.pb.js`.
    4. For each profile, generate signed "Yes" and "No" tokens.
    5. Payload: `eventId|profileId|choice|expiresAt`.
    6. Return map of tokens.
  </action>
  <verify>
    <automated>grep "/api/generate-rsvp-tokens" pocketbase/pb_hooks/token_service.pb.js</automated>
  </verify>
  <done>Tokens generated securely on the server; HMAC secret never leaves PB.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Quick RSVP Processor</name>
  <files>pocketbase/pb_hooks/quick_rsvp.pb.js</files>
  <action>
    Create `POST /api/quick-rsvp`:
    1. Public route.
    2. Input: `token`.
    3. Verify signature using `HMAC_SECRET`.
    4. Check expiration.
    5. Update `eventRosters` record for the profile/event using superuser DAO.
  </action>
  <verify>
    <automated>grep "/api/quick-rsvp" pocketbase/pb_hooks/quick_rsvp.pb.js</automated>
  </verify>
  <done>Public tokenized RSVP processing implemented.</done>
</task>

</tasks>

<success_criteria>
The backend can automatically manage singer statuses and securely process RSVPs from email links.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsvp-automation/03-02-SUMMARY.md`
</output>
