---
phase: 03.2-rsvp-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pocketbase/pb_migrations/1715690011_rsvp_automation_schema.js, pocketbase/pb_hooks/utils.pb.js]
autonomous: true
requirements: [RSVP-01, RSVP-02]
must_haves:
  truths:
    - "Profiles and Events collections have necessary fields for automation"
    - "PocketBase environment validates HMAC_SECRET existence"
  artifacts:
    - path: "pocketbase/pb_migrations/1715690011_rsvp_automation_schema.js"
      provides: "New schema fields"
    - path: "pocketbase/pb_hooks/utils.pb.js"
      provides: "Shared utility for HMAC secret validation"
---

<objective>
Establish the database schema and security foundation for RSVP and Status automation.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-3/SPEC.md
@pocketbase/pb_migrations/1715690000_collections.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create migration for RSVP and Status fields</name>
  <files>pocketbase/pb_migrations/1715690011_rsvp_automation_schema.js</files>
  <action>
    Create a new migration adding the following fields (per D-XX/SPEC):
    - `profiles` (pbc_3414089001):
      - `doNotEmail` (bool, default false)
      - `statusIsManual` (bool, default false)
      - `statusLastChangedAt` (date)
      - `statusChangeReason` (text)
    - `events` (pbc_1687431684):
      - `isOpenForRSVP` (bool, default false)
  </action>
  <verify>
    <automated>./pocketbase/pocketbase migrate up</automated>
  </verify>
  <done>Schema updated with automation fields.</done>
</task>

<task type="auto">
  <name>Task 2: Implement HMAC Secret Validation Utility</name>
  <files>pocketbase/pb_hooks/utils.pb.js</files>
  <action>
    Create a utility file that exports a function to retrieve the `HMAC_SECRET`.
    1. Check `appSettings` (pbc_app_settings_001) for `HMAC_SECRET`.
    2. If missing or empty, throw a descriptive Error ("HMAC_SECRET not configured in appSettings").
    3. Ensure NO default fallback is used (per WR-01).
  </action>
  <verify>
    <automated>grep "throw new Error" pocketbase/pb_hooks/utils.pb.js</automated>
  </verify>
  <done>Security utility ensures master secret is present before any crypto ops.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin -> App Settings | Secret entry |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | I | pb_hooks | mitigate | Ensure HMAC_SECRET is never logged or exposed in error responses to clients. |
</threat_model>

<success_criteria>
PocketBase is configured with the required schema and a strict security gate for cryptographic operations.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsvp-automation/03-01-SUMMARY.md`
</output>
