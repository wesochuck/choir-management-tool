---
phase: 03.2-rsvp-automation
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified: [src/services/communicationService.ts, src/services/profileService.ts, src/components/admin/SingerModal.tsx, src/views/PublicRsvpView.tsx]
autonomous: true
requirements: [RSVP-04, RSVP-05, RSVP-08, RSVP-09]
must_haves:
  truths:
    - "Communication composer generates RSVP links by calling the server-side API"
    - "Singer management UI supports opt-out and manual status override"
    - "Public RSVP landing page correctly handles token submission"
  artifacts:
    - path: "src/services/communicationService.ts"
      provides: "Server-side token integration"
    - path: "src/views/PublicRsvpView.tsx"
      provides: "RSVP landing page"
---

<objective>
Integrate RSVP automation and quick-rsvp functionality into the frontend UI and services.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-3/SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Communication Service for Server-Side Tokens</name>
  <files>src/services/communicationService.ts</files>
  <action>
    Modify `generateQuickRsvpLinks`:
    1. Instead of local signing, call `POST /api/generate-rsvp-tokens`.
    2. Pass the target `eventId` and `profileIds`.
    3. Map returned tokens to the `{{RSVP_LINKS}}` placeholder.
    4. Ensure the service handles the API response correctly (per CR-01).
  </action>
  <verify>
    <automated>grep "/api/generate-rsvp-tokens" src/services/communicationService.ts</automated>
  </verify>
  <done>Frontend generates secure links via the server-side token service.</done>
</task>

<task type="auto">
  <name>Task 2: Update Singer Management UI</name>
  <files>src/components/admin/SingerModal.tsx, src/services/profileService.ts</files>
  <action>
    1. Update `profileService` interfaces with new fields (`doNotEmail`, `statusIsManual`, etc.).
    2. In `SingerModal.tsx`, add toggles for `doNotEmail` and `statusIsManual`.
    3. Display read-only status info: `statusLastChangedAt` and `statusChangeReason`.
    4. Update `EventModal.tsx` (or service) to include `isOpenForRSVP` toggle.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Admin UI updated to control automation settings and view results.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Public RSVP View</name>
  <files>src/views/PublicRsvpView.tsx, src/App.tsx</files>
  <action>
    1. Create `PublicRsvpView` component that reads `?t=` from URL.
    2. On mount, call `POST /api/quick-rsvp` with the token.
    3. Display "Success" or "Error" based on response.
    4. Register route `/rsvp` in `App.tsx`.
  </action>
  <verify>
    <automated>ls src/views/PublicRsvpView.tsx</automated>
  </verify>
  <done>Public RSVP landing page implemented and routed.</done>
</task>

</tasks>

<success_criteria>
The end-to-end flow for secure, automated status and RSVP is complete and user-facing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-rsvp-automation/03-03-SUMMARY.md`
</output>
