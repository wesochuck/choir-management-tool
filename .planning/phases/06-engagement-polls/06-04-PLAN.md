---
phase: 06-engagement-polls
plan: 04
type: execute
wave: 3
depends_on: [06-02]
files_modified: [src/views/admin/PollsDashboardView.tsx, src/components/admin/PollSummaryList.tsx, src/App.tsx]
autonomous: true
requirements: [POLL-06, POLL-07]
must_haves:
  truths:
    - "Admin can see aggregate volunteer counts for each poll."
    - "Admin can see the names of singers who volunteered."
    - "Old polls linked to past events are archived/hidden by default."
  artifacts:
    - path: "src/views/admin/PollsDashboardView.tsx"
      provides: "Admin dashboard for poll responses"
---

<objective>
Implement the admin dashboard for reviewing poll responses and volunteer counts.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-engagement-polls/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Polls Dashboard View</name>
  <files>src/views/admin/PollsDashboardView.tsx, src/components/admin/PollSummaryList.tsx</files>
  <action>
    1. Create `src/views/admin/PollsDashboardView.tsx`.
    2. Implement `PollSummaryList.tsx` to display polls grouped by Event.
    3. Show "Volunteer Count" (Number of "Yes" responses).
    4. Allow clicking a poll to see the list of names for "Yes" and "No" responses.
    5. Implement auto-archive logic: Filter list by `event.date > today` or `eventId = ""`. Add a toggle to "Show Archived".
  </action>
  <verify>
    <automated>ls src/views/admin/PollsDashboardView.tsx</automated>
  </verify>
  <done>Polls dashboard implemented with reporting and archiving.</done>
</task>

<task type="auto">
  <name>Task 2: Register Dashboard Route</name>
  <files>src/App.tsx</files>
  <action>
    1. Register `/admin/polls` in `src/App.tsx`.
    2. Add a navigation link to the Sidebar or Communications area (if applicable).
  </action>
  <verify>
    <automated>grep -r "/admin/polls" src/App.tsx</automated>
  </verify>
  <done>Polls dashboard is accessible via navigation.</done>
</task>

</tasks>

<success_criteria>
- Volunteer counts are accurate.
- Archiving logic correctly hides past event polls.
</success_criteria>

<output>
After completion, create .planning/phases/06-engagement-polls/06-04-SUMMARY.md
</output>
