---
phase: 06-engagement-polls
plan: 05
type: execute
wave: 4
depends_on: [06-02]
files_modified: [src/views/singer/DashboardView.tsx, src/services/pollService.ts]
autonomous: true
requirements: [POLL-07]
must_haves:
  truths:
    - "Logged-in singers can view and respond to polls on their dashboard."
    - "The list only shows 'Active' polls (not archived/past event polls)."
---

<objective>
Add an "Open Polls" section to the Singer Dashboard to allow logged-in members to view and respond to questions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-engagement-polls/06-CONTEXT.md
@src/views/singer/DashboardView.tsx
@src/services/pollService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update pollService for Logged-in Access</name>
  <files>src/services/pollService.ts</files>
  <action>
    Add a method `getActivePollsForSinger(profileId: string)` that:
    1. Fetches all polls where eventId is null OR linked to a future event.
    2. Fetches the singer's current responses to those polls.
    3. Merges them into a list of polls with 'status' (Yes/No/None).
  </action>
  <verify>
    <automated>npm run test src/services/pollService.test.ts</automated>
  </verify>
  <done>pollService supports dashboard integration.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Dashboard UI Section</name>
  <files>src/views/singer/DashboardView.tsx</files>
  <action>
    1. Add a new `AppCard` or section labeled "📊 Quick Polls".
    2. List active polls found via `pollService`.
    3. For each poll, show the question and a "Volunteer" toggle button.
    4. Provide immediate feedback (optimistic UI update) when the button is clicked.
  </action>
  <verify>
    <automated>ls src/views/singer/DashboardView.tsx</automated>
  </verify>
  <done>Singer dashboard now displays and handles engagement polls.</done>
</task>

</tasks>

<success_criteria>
- Singers can see active polls without needing an email link.
- Responses made in the dashboard are reflected in the admin Polls Dashboard.
</success_criteria>

<output>
After completion, create .planning/phases/06-engagement-polls/06-05-SUMMARY.md
</output>
