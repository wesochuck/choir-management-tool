---
phase: 06-engagement-polls
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: [src/services/pollService.ts, src/services/communicationService.ts, src/views/singer/PublicPollView.tsx, src/App.tsx, src/lib/communicationUtils.ts]
autonomous: true
requirements: [POLL-03, POLL-04]
must_haves:
  truths:
    - "Singer can view poll details using a valid token."
    - "Singer can submit a 'Volunteer' response."
  artifacts:
    - path: "src/services/pollService.ts"
      provides: "Frontend API for polls"
    - path: "src/views/singer/PublicPollView.tsx"
      provides: "Poll response landing page"
---

<objective>
Implement the service layer and the singer-facing landing page for poll responses.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-engagement-polls/06-01-SUMMARY.md
@src/services/communicationService.ts
@src/views/singer/PublicRsvpView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create poll service and update communication service</name>
  <files>src/services/pollService.ts, src/services/communicationService.ts, src/lib/communicationUtils.ts</files>
  <action>
    1. Create `src/services/pollService.ts` to wrap the backend /api/poll-* endpoints.
    2. Add `resolvePollPlaceholders` to `communicationService.ts` (analogous to `resolveRsvpPlaceholders`). It should detect `{{POLL_LINK:pollId}}` and call `/api/generate-poll-tokens`.
    3. Update `src/lib/communicationUtils.ts` to handle `{{POLL_LINK:pollId}}` placeholder in previews.
  </action>
  <verify>
    <automated>ls src/services/pollService.ts</automated>
  </verify>
  <done>Service layer implemented and communication service updated to support poll links.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Public Poll View</name>
  <files>src/views/singer/PublicPollView.tsx, src/App.tsx</files>
  <action>
    1. Create `src/views/singer/PublicPollView.tsx`.
    2. Use `useSearchParams` to extract the `token`.
    3. Fetch poll details via `pollService.getPollDetails(token)`.
    4. Display question and a large "Volunteer" toggle (Yes/No).
    5. On toggle change, call `pollService.submitResponse(token, status)`.
    6. Register the route `/poll` in `src/App.tsx` (unprotected).
  </action>
  <verify>
    <automated>grep -r "/poll" src/App.tsx</automated>
  </verify>
  <done>Public poll view is accessible and functional.</done>
</task>

</tasks>

<success_criteria>
- Poll details are correctly fetched and displayed.
- Responses are persisted to the database via the backend hook.
</success_criteria>

<output>
After completion, create .planning/phases/06-engagement-polls/06-02-SUMMARY.md
</output>
