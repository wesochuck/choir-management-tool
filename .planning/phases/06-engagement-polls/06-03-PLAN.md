---
phase: 06-engagement-polls
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified: [src/views/admin/CommunicationView.tsx, src/components/admin/PollSelectionModal.tsx, src/components/admin/PlaceholderPanel.tsx]
autonomous: true
requirements: [POLL-05]
must_haves:
  truths:
    - "Admin can insert a poll placeholder into the message content."
    - "Admin can create a new poll on the fly from the communications view."
  artifacts:
    - path: "src/components/admin/PollSelectionModal.tsx"
      provides: "Poll selection and creation UI"
---

<objective>
Integrate poll creation and selection into the Communication composer.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/06-engagement-polls/06-02-SUMMARY.md
@src/views/admin/CommunicationView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Poll Selection Modal</name>
  <files>src/components/admin/PollSelectionModal.tsx</files>
  <action>
    Create a modal component that allows admins to:
    1. Select an existing poll.
    2. Create a new poll (Question + optional Event link).
    On selection, it should return the placeholder `{{POLL_LINK:pollId}}`.
  </action>
  <verify>
    <automated>ls src/components/admin/PollSelectionModal.tsx</automated>
  </verify>
  <done>Poll selection modal created.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate into Communication View</name>
  <files>src/views/admin/CommunicationView.tsx, src/components/admin/PlaceholderPanel.tsx</files>
  <action>
    1. Update `CommunicationView.tsx` to include an "📊 Insert Poll" button in the composer toolbar or actions.
    2. Open `PollSelectionModal` when clicked.
    3. Insert the returned placeholder at the cursor position in the textarea.
    4. Update `PlaceholderPanel.tsx` to list the poll placeholder as an option.
  </action>
  <verify>
    <automated>grep -i "Insert Poll" src/views/admin/CommunicationView.tsx</automated>
  </verify>
  <done>Poll integration added to the communication composer UI.</done>
</task>

</tasks>

<success_criteria>
- Admin can successfully insert a poll placeholder.
- New polls can be created directly from the communication view.
</success_criteria>

<output>
After completion, create .planning/phases/06-engagement-polls/06-03-SUMMARY.md
</output>
