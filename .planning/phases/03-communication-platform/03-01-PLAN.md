# Plan: Communication Platform

## Tasks

### 1. Data Model
- Add `pocketbase/pb_migrations/1715690009_messages.js`.
- Create `messages` collection with subject, content, type, recipients, filters, optional sender, created, and updated fields.
Status: Complete.

### 2. Service Layer
- Create `src/services/communicationService.ts`.
- Implement `getMessages`, `saveMessage`, `sendBulkMessage`, `resolveRecipients`, and communication config helpers.
Status: Complete.

### 3. Admin UI
- Create `src/views/admin/CommunicationView.tsx`.
- Implement Compose, History, and Settings tabs.
- Wire filtered recipients, individual selection, preview, send logging, copy-to-draft, and config save.
Status: Complete.

### 4. Navigation
- Add `/admin/communications` route.
- Add dashboard entry.
Status: Complete.

### 5. Verification
- Run `npm run build`, `npm run lint`, `npm test`.
- Apply migrations against current and fresh PocketBase data.
- Smoke test `messages` create/read/delete and config save/read.
Status: Complete.
