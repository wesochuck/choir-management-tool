# Phase 3: Singer UI

## Tasks
- [ ] Task 3.1: Update `src/views/PublicRsvpView.tsx`.
  - Add `rsvpNote` state.
  - Hydrate from `/api/rsvp-details`.
  - Add textarea for note (visible when Rehearsal + No).
  - Add client-side validation for presence and length.
  - Send `rsvpNote` to `/api/quick-rsvp`.

## Read First
- `src/views/PublicRsvpView.tsx`

## Verification
- Manual check of the UI (if possible) or code review.
