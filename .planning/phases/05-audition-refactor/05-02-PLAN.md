---
phase: 05-audition-refactor
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/views/admin/AuditionsView.tsx, src/components/admin/AuditionModal.tsx, src/views/admin/SettingsView.tsx]
autonomous: true
requirements: [AUDIT-02, AUDIT-03]
must_haves:
  truths:
    - "Audition settings are visible and editable in AuditionsView"
    - "Auditions can be filtered by performance in AuditionsView"
    - "Audition settings are no longer in SettingsView"
    - "AuditionModal allows changing the associated performance"
  artifacts:
    - path: "src/views/admin/AuditionsView.tsx"
      provides: "Integrated settings and filtering UI"
    - path: "src/components/admin/AuditionModal.tsx"
      provides: "Updated modal with performance selection"
    - path: "src/views/admin/SettingsView.tsx"
      provides: "Cleaner settings UI without auditions section"
---

<objective>
Refactor the Admin UI to centralize audition management and settings, and add the ability to associate and filter auditions by performance.

Purpose: Improve administrative efficiency by bringing all audition-related controls into a single dashboard.
Output: Updated AuditionsView, AuditionModal, and SettingsView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audition-refactor/SPEC.md
@src/views/admin/AuditionsView.tsx
@src/components/admin/AuditionModal.tsx
@src/views/admin/SettingsView.tsx
@src/services/eventService.ts

<interfaces>
From src/services/eventService.ts:
```typescript
export interface Event extends RecordModel {
  title: string;
  date: string;
  type: 'Performance' | 'Rehearsal';
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate settings into AuditionsView</name>
  <files>src/views/admin/AuditionsView.tsx</files>
  <action>
    - Import `settingsService` and `eventService`.
    - Fetch audition settings and list of performance events on mount.
    - Add a collapsible or top-level section for Audition Settings (Enabled toggle, Available Slots textarea, Confirmation Message textarea, and Default Performance dropdown).
    - Implement a "Save Settings" button that calls `settingsService.saveAuditionSettings`.
    - Add a "Performance" filter dropdown above the auditions list to filter the displayed `auditions` state.
    - Display the associated performance title in each audition row if available.
  </action>
  <verify>
    <automated>grep "settingsService" src/views/admin/AuditionsView.tsx</automated>
  </verify>
  <done>AuditionsView has settings controls and performance filtering</done>
</task>

<task type="auto">
  <name>Task 2: Update AuditionModal with performance selection</name>
  <files>src/components/admin/AuditionModal.tsx</files>
  <action>
    - Import `eventService` and fetch performance events.
    - Add a "Performance" dropdown to the modal form.
    - Ensure the current `audition.performance` is selected by default.
    - Include the selected performance ID in the `onSave` call.
  </action>
  <verify>
    <automated>grep "eventService" src/components/admin/AuditionModal.tsx</automated>
  </verify>
  <done>AuditionModal allows editing the performance association</done>
</task>

<task type="auto">
  <name>Task 3: Remove audition settings from SettingsView</name>
  <files>src/views/admin/SettingsView.tsx</files>
  <action>
    - Remove the `AppCard` for "Auditions".
    - Remove unused state (`auditionSettings`, `slotText`) and imports related to audition settings.
    - Ensure the "Save Settings" button still works for other settings.
  </action>
  <verify>
    <automated>grep -v "Auditions" src/views/admin/SettingsView.tsx</automated>
  </verify>
  <done>SettingsView is cleaned up</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin UI -> Services | Authenticated admin operations |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Elevation of Privilege | Settings save | mitigate | Protected by PocketBase admin rules. |
</threat_model>

<verification>
Check visual layout of AuditionsView.
Verify filtering logic (should show all if "All Performances" selected).
Verify settings save correctly from AuditionsView.
</verification>

<success_criteria>
- AuditionsView contains all settings previously in SettingsView.
- AuditionsView allows filtering by performance.
- AuditionModal allows changing the performance.
- SettingsView no longer has the Auditions section.
</success_criteria>

<output>
After completion, create `.planning/phases/05-audition-refactor/05-02-SUMMARY.md`
</output>
