---
phase: 05-audition-refactor
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [src/views/PublicAuditionView.tsx]
autonomous: true
requirements: [AUDIT-04]
must_haves:
  truths:
    - "New audition requests are associated with the default performance"
    - "The public form handles the absence of a default performance gracefully"
  artifacts:
    - path: "src/views/PublicAuditionView.tsx"
      provides: "Updated public form with performance assignment"
---

<objective>
Update the public audition request form to automatically associate new requests with the default performance set by the administrator.

Purpose: Ensure data consistency and enable per-performance management of audition requests from the moment of submission.
Output: Updated PublicAuditionView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audition-refactor/SPEC.md
@src/views/PublicAuditionView.tsx
@src/services/settingsService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PublicAuditionView with performance assignment</name>
  <files>src/views/PublicAuditionView.tsx</files>
  <action>
    - Read `defaultPerformanceId` from the loaded audition settings.
    - Pass this ID to `auditionService.createAudition` when the form is submitted.
    - If `defaultPerformanceId` is missing, allow submission without it (graceful fallback) as the field is optional in the schema.
  </action>
  <verify>
    <automated>grep "defaultPerformanceId" src/views/PublicAuditionView.tsx</automated>
  </verify>
  <done>Public form assigns default performance to new requests</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>End-to-end audition refactor</what-built>
  <how-to-verify>
    1. Log in as admin and go to Auditions.
    2. Set a default performance in the new settings section and save.
    3. Open the public audition form and submit a new request.
    4. Go back to the admin Auditions view.
    5. Verify the new request appears and is correctly associated with the performance.
    6. Try filtering the auditions list by performance.
    7. Verify Audition settings are gone from the general Settings view.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public Form -> PocketBase | Public creation of audition requests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | performance ID | mitigate | The performance ID is taken from server-side settings; users cannot easily manipulate it without knowledge of event IDs. |
</threat_model>

<verification>
Submit an audition through the public form.
Check the network tab to ensure `performance` field is sent in the POST request.
Check the admin dashboard to see if it shows up with the performance name.
</verification>

<success_criteria>
- Public submissions include the performance ID.
- Admin dashboard shows performance association for new requests.
- Full cycle (Settings -> Public Form -> Admin List) works as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/05-audition-refactor/05-03-SUMMARY.md`
</output>
