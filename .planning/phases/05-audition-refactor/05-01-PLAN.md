---
phase: 05-audition-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pocketbase/pb_migrations/1715690016_audition_performance_relation.js, src/services/settingsService.ts, src/services/auditionService.ts]
autonomous: true
requirements: [AUDIT-01, AUDIT-02]
must_haves:
  truths:
    - "The auditions collection has a performance relation field"
    - "settingsService supports defaultPerformanceId"
    - "auditionService supports performance in Audition records"
  artifacts:
    - path: "pocketbase/pb_migrations/1715690016_audition_performance_relation.js"
      provides: "Schema update for auditions"
    - path: "src/services/settingsService.ts"
      provides: "Updated settings types"
    - path: "src/services/auditionService.ts"
      provides: "Updated audition types and service"
---

<objective>
Update the database schema and service layers to support associating audition requests with specific performance events.

Purpose: Provide the technical foundation for filtering auditions by performance and automating performance assignment in the public form.
Output: Migration file and updated service code.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-audition-refactor/SPEC.md
@src/services/settingsService.ts
@src/services/auditionService.ts
@pocketbase/pb_migrations/1715690005_auditions.js

<interfaces>
From src/services/settingsService.ts:
```typescript
export interface AuditionSettings {
  enabled: boolean;
  slots: string[];
  confirmationMessage: string;
}
```

From src/services/auditionService.ts:
```typescript
export interface Audition extends RecordModel {
  name: string;
  contact: string;
  timeSlot: string;
  voicePart?: 'S1' | 'S2' | 'A1' | 'A2' | 'T1' | 'T2' | 'B1' | 'B2';
  experience?: string;
  status: 'New' | 'Contacted' | 'Scheduled' | 'Closed';
  notes: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create schema migration</name>
  <files>pocketbase/pb_migrations/1715690016_audition_performance_relation.js</files>
  <action>
    Create a new migration file to add the `performance` field to the `auditions` collection (pbc_auditions_001).
    - Field name: `performance`
    - Type: `relation`
    - Collection ID: `pbc_1687431684` (events)
    - Cascade delete: false
    - Max select: 1
    - Required: false (to maintain compatibility with existing records)
  </action>
  <verify>
    <automated>grep "performance" pocketbase/pb_migrations/1715690016_audition_performance_relation.js</automated>
  </verify>
  <done>Migration file created with correct parameters</done>
</task>

<task type="auto">
  <name>Task 2: Update settingsService types and defaults</name>
  <files>src/services/settingsService.ts</files>
  <action>
    Update `AuditionSettings` interface to include `defaultPerformanceId?: string`.
    Update `DEFAULT_AUDITION_SETTINGS` to include `defaultPerformanceId: ''`.
    Ensure the `settingsService` methods correctly handle this field (they should automatically as they use JSON serialization for the `value` field in `appSettings`).
  </action>
  <verify>
    <automated>grep "defaultPerformanceId" src/services/settingsService.ts</automated>
  </verify>
  <done>AuditionSettings interface and defaults updated</done>
</task>

<task type="auto">
  <name>Task 3: Update auditionService types and CRUD</name>
  <files>src/services/auditionService.ts</files>
  <action>
    Update `Audition` interface to include `performance?: string`.
    Update `createAudition` signature and implementation to accept and save the `performance` field.
    Update `getAuditions` to expand the `performance` relation.
  </action>
  <verify>
    <automated>grep "performance" src/services/auditionService.ts</automated>
  </verify>
  <done>Audition interface and service methods updated</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Public Form -> PocketBase | Public creation of audition requests |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | audition request | mitigate | PocketBase schema restricts fields; only allowed fields are accepted. |
| T-05-02 | Information Disclosure | performance ID | accept | Performance IDs are public via the events list anyway. |
</threat_model>

<verification>
Run `npm run build` to ensure no type errors in services.
If possible, run a manual check of the migration (not automated in this step).
</verification>

<success_criteria>
- Migration file exists.
- Service interfaces include `performance` and `defaultPerformanceId`.
- No TypeScript errors in services.
</success_criteria>

<output>
After completion, create `.planning/phases/05-audition-refactor/05-01-SUMMARY.md`
</output>
