---
phase: 03-seating-print-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/stringUtils.ts, src/views/admin/SeatingView.tsx, src/App.css]
autonomous: true
requirements: [PRNT-01, PRNT-02, PRNT-03]

must_haves:
  truths:
    - "Admin can toggle between Visual and Text-only modes via a switch in the toolbar."
    - "Text-only mode shows a list where Row 1 is labeled '(Back)' and the final row is '(Front)'."
    - "Names in text mode correctly handle suffixes (e.g., 'Smith Jr.' instead of 'Jr.')."
    - "Print output is isolated: Text mode print does not show the grid; Visual mode print does not show the text list."
    - "Render performance remains high for large rosters (pre-grouped row data)."
  artifacts:
    - path: "src/lib/stringUtils.ts"
      provides: "getLastName helper with suffix support"
    - path: "src/views/admin/SeatingView.tsx"
      provides: "Toggle logic, pre-grouping hook, and SeatingTextList component"
    - path: "src/App.css"
      provides: "Scoped @media print rules using data-print-mode"
  key_links:
    - from: "SeatingView.tsx"
      to: "stringUtils.ts"
      via: "import { getLastName }"
    - from: "SeatingView.tsx"
      to: "App.css"
      via: "data-print-mode attribute"
---

<objective>
Implement a robust, high-performance Seating Chart Print Mode that addresses row ordering contradictions, complex name parsing, and print style isolation.

Purpose: Provide directors with clear, physically accurate reference sheets for rehearsals and performances.
Output: Integrated mode toggle, improved name parsing utility, and isolated print styles.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-seating-print-mode/SPEC.md
@src/views/admin/SeatingView.tsx
@src/components/admin/SeatingGrid.tsx
@src/App.css

<interfaces>
From src/services/profileService.ts:
```typescript
export interface Profile {
  id: string;
  name: string;
  voicePart: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utilities and Pre-grouping Logic</name>
  <files>src/lib/stringUtils.ts, src/views/admin/SeatingView.tsx</files>
  <action>
    - Create `src/lib/stringUtils.ts` with a `getLastName(fullName: string)` function.
      - Use a whitelist of suffixes (Jr, Sr, II, III, IV, V) to ensure "John Smith Jr" returns "Smith Jr" not "Jr".
    - In `SeatingView.tsx`, implement a `useMemo` hook to pre-group `optimisticAssignments` into a `rows` structure:
      - `const groupedRows = Profile[][]` where `groupedRows[rowIndex][seatIndex]` is the Profile.
      - This avoids O(N^2) lookups during the render cycle of the text list.
    - Reference Locked Decision D-01: Ensure this grouping respects the latest `optimisticAssignments` state.
  </action>
  <verify>
    <automated>
      # Verify utility exists and handles suffixes
      node -e 'const {getLastName} = require("./src/lib/stringUtils.ts"); console.assert(getLastName("John Smith Jr") === "Smith Jr")'
    </automated>
  </verify>
  <done>Core data utilities and performance optimizations implemented.</done>
</task>

<task type="auto">
  <name>Task 2: UI Toggle and Text List Component</name>
  <files>src/views/admin/SeatingView.tsx</files>
  <action>
    - Add `printMode` state ('visual' | 'text', default 'visual') to `SeatingView`.
    - Implement a toggle UI in the SeatingView toolbar (near the Print button).
    - Implement `SeatingTextList` component (local or sub-component):
      - It takes the pre-grouped `rows` data.
      - Renders Row 1 at the top, labeled as "Row 1 (Back)".
      - Renders the last row labeled as "Row N (Front)".
      - Uses `getLastName` for display.
      - Ensure Row 1 corresponds to the furthest row from the director (top of the grid).
    - Apply `data-print-mode={printMode}` to the main container in `SeatingView`.
  </action>
  <verify>
    <automated>grep -q "data-print-mode" src/views/admin/SeatingView.tsx && grep -q "getLastName" src/views/admin/SeatingView.tsx</automated>
  </verify>
  <done>Toggle UI and Text-only rendering logic completed with correct row orientation.</done>
</task>

<task type="auto">
  <name>Task 3: Scoped Print Styles</name>
  <files>src/App.css</files>
  <action>
    - Refactor `@media print` section in `App.css` to use scoped selectors:
      - `[data-print-mode="text"] .grid-print { display: none !important; }`
      - `[data-print-mode="visual"] .seating-text-list { display: none !important; }`
    - Ensure `.no-print` correctly hides navigation, buttons, and sidebars in ALL modes.
    - Ensure high-contrast (black on white) rules apply to the text list.
    - Add page-break-inside rules for the text list to prevent rows from splitting across pages.
  </action>
  <verify>
    <automated>grep -q "data-print-mode=\"text\"" src/App.css</automated>
  </verify>
  <done>Print styles isolated, preventing regressions between visual and text modes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
- Browser Print Engine (System Boundary)

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Print Output | mitigate | Standard `.no-print` usage for all non-essential UI (ids, controls). |
| T-03-02 | Denial of Service | Render Loop | mitigate | Pre-group assignments in useMemo (Task 1) to avoid O(N^2) rendering. |
</threat_model>

<verification>
1. Select a performance and venue.
2. Assign several singers.
3. Toggle "Text-only" mode.
4. Verify Row 1 is labeled "(Back)" and shows correct last names (check suffixes).
5. Open Print Preview (Cmd+P) in "Text-only" mode: Verify NO grid is visible.
6. Toggle back to "Visual" mode.
7. Open Print Preview: Verify grid IS visible and text list IS NOT visible.
</verification>

<success_criteria>
- Toggle correctly switches between visual and text list UI.
- Text list shows physically accurate row labels (Row 1 = Back).
- Last names are parsed correctly with suffix support.
- Print output is strictly isolated to the selected mode.
- No performance lag observed with full choir assignments.
</success_criteria>

<output>
After completion, create `.planning/phases/03-seating-print-mode/03-01-SUMMARY.md`
</output>
