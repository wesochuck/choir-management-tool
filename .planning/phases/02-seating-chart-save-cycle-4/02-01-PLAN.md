---
phase: 02-seating-chart-save-cycle-4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hooks/useSeatingChart.ts]
autonomous: true
requirements: [SEATING-01, SEATING-02]
must_haves:
  truths:
    - "Every edit increments lastEditIdRef synchronously"
    - "Sync captures current lastEditIdRef as requestId"
    - "Responses only update lastAppliedIdRef if requestId > lastAppliedIdRef"
    - "isDirty is true if lastEditIdRef > lastAppliedIdRef"
    - "isSaving includes activeRequestsCount and debounce pending"
---

<objective>
Implement the robust state machine in `useSeatingChart` to handle save concurrency and out-of-order network responses.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-2/SPEC.md
@src/hooks/useSeatingChart.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor useSeatingChart state and refs</name>
  <files>src/hooks/useSeatingChart.ts</files>
  <action>
    - Replace `isDirty` state with a derived value from `lastEditIdRef` and `lastAppliedIdRef`.
    - Add `activeRequestsCount` (state) to track in-flight requests.
    - Add `lastEditIdRef` (number, initial 0) and `lastAppliedIdRef` (number, initial 0).
    - Simplify `optimisticAssignments` management to be the primary local state.
    - Remove redundant refs like `dirtyVersionRef`, `isDirtyRef`, `dirtyPayloadRef`.
  </action>
  <verify>Code compiles and state variables are declared correctly.</verify>
  <done>State and refs aligned with SPEC.md.</done>
</task>

<task type="auto">
  <name>Task 2: Implement core syncWithServer logic</name>
  <files>src/hooks/useSeatingChart.ts</files>
  <action>
    - Update `syncWithServer` to:
      1. Capture `const requestId = lastEditIdRef.current`.
      2. Clear debounce timer.
      3. Increment `activeRequestsCount`.
      4. Reset `error` to null.
      5. Call `seatingService.saveChart`.
      6. In `.then()`: if `requestId > lastAppliedIdRef.current`, update `lastAppliedIdRef.current = requestId` and update `chart` state.
      7. In `.catch()`: if `requestId === lastEditIdRef.current`, set `error`.
      8. In `.finally()`: decrement `activeRequestsCount`.
  </action>
  <verify>Automated tests or manual verification of the save flow.</verify>
  <done>Save logic handles out-of-order responses and error clearing per SPEC.md.</done>
</task>

<task type="auto">
  <name>Task 3: Update edit methods to increment sequence</name>
  <files>src/hooks/useSeatingChart.ts</files>
  <action>
    - In `assignSinger` and `updateChart`:
      1. Increment `lastEditIdRef.current` FIRST.
      2. Update `optimisticAssignments` synchronously.
      3. Schedule debounced `syncWithServer`.
  </action>
  <verify>Every edit triggers an increment and a debounced sync.</verify>
  <done>Edits are tracked by sequence IDs immediately.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → PocketBase | Seating chart updates sent to server |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `saveChart` | mitigate | Server-side validation of performance/venue IDs. |
| T-02-02 | Information Disclosure | `getChartForPerformance` | mitigate | Ensure ACLs prevent unauthorized viewing of seating charts. |
</threat_model>

<success_criteria>
- Rapid edits do not result in data loss.
- Network errors are cleared on retry.
- Out-of-order responses do not overwrite newer local changes.
</success_criteria>
