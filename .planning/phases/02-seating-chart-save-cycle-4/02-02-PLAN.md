---
phase: 02-seating-chart-save-cycle-4
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [src/views/admin/SeatingView.tsx]
autonomous: true
requirements: [SEATING-01, SEATING-02]
must_haves:
  truths:
    - "Manual save button shows 'Saved' if isDirty is false"
    - "Manual save button shows 'Save failed' if error exists"
    - "Manual save button shows 'Saving...' if isSaving is true"
    - "Force save ignores debounce and triggers sync immediately"
---

<objective>
Update the seating chart toolbar in `SeatingView` to display the correct save status and handle the manual force-save action.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-2/SPEC.md
@src/views/admin/SeatingView.tsx
@src/hooks/useSeatingChart.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SeatingView save button and status text</name>
  <files>src/views/admin/SeatingView.tsx</files>
  <action>
    - Refactor the Save button/status area to use the new flags from `useSeatingChart`:
      - If `error`: Show error message and "Retry Save" button.
      - If `isSaving`: Show "Saving..." indicator.
      - If `isDirty`: Show "Unsaved Changes" and "Save Now" button.
      - Else: Show "Saved" indicator and a disabled "Save" button.
    - Ensure `forceSave` is called when the manual button is clicked.
  </action>
  <verify>Manual visual verification of status transitions.</verify>
  <done>UI reflects the internal state machine accurately.</done>
</task>

<task type="auto">
  <name>Task 2: Implement success toast for manual save</name>
  <files>src/views/admin/SeatingView.tsx</files>
  <action>
    - When `forceSave` is called and `isDirty` is false, show a temporary "All changes saved" toast or indicator.
  </action>
  <verify>Clicking Save when no changes exist shows a feedback message.</verify>
  <done>User receives feedback for manual save clicks even when clean.</done>
</task>

</tasks>

<success_criteria>
- User sees clear feedback during saving, error, and idle states.
- Manual save button behaves as expected.
</success_criteria>
