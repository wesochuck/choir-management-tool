---
phase: 05-polish-and-paging
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified: [
  "src/views/admin/EventsView.tsx",
  "src/views/admin/RosterView.tsx",
  "src/views/admin/AuditionsView.tsx",
  "src/views/admin/EventRosterView.tsx",
  "src/components/admin/CheckInList.tsx"
]
autonomous: true
requirements: [
  "enhanced navigation jumps",
  "deep linking"
]
must_haves:
  truths:
    - "Admin can jump directly to an event's editor from the Event Roster or Auditions views."
    - "Admin can jump directly to a singer's profile from the Attendance check-in list."
    - "URL search parameters trigger specific modals to open on view load."
---

<objective>
Enhance the connectivity of the admin dashboard by implementing cross-module "jumps" and deep linking.

Purpose: Reduce clicks and context switching when managing related entities (e.g., from an RSVP list to the event details).
Output: Deep-link support in Events and Roster views, and navigation shortcuts in Auditions, Attendance, and Event Roster.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-paging/05-03-SUMMARY.md
@src/views/admin/EventsView.tsx
@src/views/admin/RosterView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 4.1: Deep Linking for Events and Roster</name>
  <files>src/views/admin/EventsView.tsx, src/views/admin/RosterView.tsx</files>
  <action>
    1. In `EventsView.tsx`, use `useSearchParams` to check for `eventId` and `openModal=true`. If present, find the event and set it as `editingEvent` and set `isModalOpen` to `true`.
    2. In `RosterView.tsx`, similarly check for `singerId` and `openModal=true`. Use `allProfiles` from `useProfiles` hook to find the singer and open `SingerModal`.
  </action>
  <verify>
    <automated>Navigate to /admin/events?eventId=some-id&openModal=true and check if the modal opens.</automated>
  </verify>
  <done>Events and Roster views support opening specific edit modals via URL parameters.</done>
</task>

<task type="auto">
  <name>Task 4.2: Add Navigation Jumps from Other Views</name>
  <files>src/components/admin/CheckInList.tsx, src/views/admin/AuditionsView.tsx, src/views/admin/EventRosterView.tsx</files>
  <action>
    1. In `CheckInList.tsx`: Make the singer name clickable. On click, navigate to `/admin/roster?singerId={item.profileId}&openModal=true`.
    2. In `EventRosterView.tsx`: Add a "Edit Event" button near the event title that navigates to `/admin/events?eventId={eventId}&openModal=true`.
    3. In `AuditionsView.tsx`: Add a link or button on each audition row to "Edit Performance" which navigates to the specific event deep link.
    4. In `AuditionsView.tsx`: Add an "Email Applicant" button that navigates to `/admin/communications` with state: `{ initialRecipients: [{ name: applicant.name, email: applicant.email }] }`.
  </action>
  <verify>
    <automated>Verify that clicking the singer name in Attendance view redirects to the Roster with the modal open.</automated>
  </verify>
  <done>One-click navigation jumps are implemented across key admin modules.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| URL → Component | Search params are untrusted; components must validate ID existence before opening modals |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | EventsView | mitigate | Only open modals for IDs the current user has access to view (PB will enforce this via collection rules) |
</threat_model>

<verification>
Ensure navigation between modules is smooth and correctly opens the target modal.
Verify that invalid IDs in search params don't crash the app.
</verification>

<output>
After completion, create `.planning/phases/05-polish-and-paging/05-04-SUMMARY.md`
</output>
