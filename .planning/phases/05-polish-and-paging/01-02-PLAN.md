---
phase: 05-polish-and-paging
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [
  "src/services/communicationService.ts",
  "src/components/common/Pagination.tsx",
  "src/views/admin/CommunicationView.tsx",
  "src/components/admin/MessageHistory.tsx"
]
autonomous: true
requirements: [
  "automation integrity",
  "server-side pagination",
  "communication history section"
]
must_haves:
  truths:
    - "Communication history is paginated on the server side."
    - "Automated message detection (sent check) works correctly even with paginated history."
    - "Pagination component is reusable and accessible."
  artifacts:
    - path: "src/components/common/Pagination.tsx"
      provides: "Shared UI for navigation through lists"
---

<objective>
Implement a reusable pagination component and refactor the communication system to support server-side paging without breaking automated task detection.

Purpose: Solve the technical risk of paginated history breaking sent-message checks and provide the foundation for scalable list views.
Output: Reusable Pagination component and pagination-resilient CommunicationView.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-paging/05-REVIEWS.md
@src/services/communicationService.ts
@src/views/admin/CommunicationView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 2.1: Reusable Pagination Component</name>
  <files>src/components/common/Pagination.tsx</files>
  <action>
    Create a `Pagination` component that:
    - Accepts `currentPage`, `totalPages`, `onPageChange`.
    - Renders First/Prev/Next/Last and numeric page buttons.
    - Uses a sliding window for page numbers (e.g., `1 ... 4 5 6 ... 20`).
    - Is accessible (aria-labels, current state).
  </action>
  <verify>
    <automated>npm run test src/components/common/Pagination.test.tsx</automated>
  </verify>
  <done>Reusable pagination component is available for all list views.</done>
</task>

<task type="auto">
  <name>Task 2.2: Automation Integrity & Sent Status Check</name>
  <files>src/services/communicationService.ts</files>
  <behavior>
    - wasMessageSent({ eventId: 'E1', type: 'Reminder' }) returns true if any Sent message matches both.
  </behavior>
  <action>
    1. Update `getMessages` to `getMessagesPaginated(page, perPage, filterString)`. Use `recipientIds ~ {:filterSingerId}` if filtering by recipient.
    2. Add `wasMessageSent(filter: { eventId?: string, type?: string, rsvp?: string })` to `communicationService`.
    3. It should use `pb.collection('messages').getFirstListItem` with a targeted filter string: `status = "Sent" && filters.eventId = "{eventId}" && filters.type = "{type}"`.
  </action>
  <verify>
    <automated>npm run test src/services/communicationService.test.ts</automated>
  </verify>
  <done>Communication service supports paginated history and targeted sent-status verification.</done>
</task>

<task type="auto">
  <name>Task 2.3: Paginated Communication History & Task Refactor</name>
  <files>src/views/admin/CommunicationView.tsx, src/components/admin/MessageHistory.tsx</files>
  <action>
    1. Refactor the `useMemo` for `upcomingTasks`/`pastTasks` in `CommunicationView.tsx`.
    2. Instead of checking the `history` state (which will be paginated), fetch the "sent statuses" for all visible events on mount or when the `Automated` tab is focused.
    3. Update `MessageHistory.tsx` to use the new `Pagination` component and trigger `onPageChange` which re-fetches data from the service.
  </action>
  <verify>
    <automated>Check that automated tasks still show as 'Sent' even if the message is on page 2 of history.</automated>
  </verify>
  <done>Communication view shows server-paginated history and correctly identifies sent automated messages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → API | Filter parameters passed to PB are sanitized via `pb.filter` |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | communicationService | mitigate | Implement pagination with reasonable `perPage` defaults (e.g., 20) |
</threat_model>

<verification>
Ensure 'Sent' status on automated tasks is persistent across history page changes.
Verify pagination buttons correctly fetch the next set of messages.
</verification>

<output>
After completion, create `.planning/phases/05-polish-and-paging/05-02-SUMMARY.md`
</output>
