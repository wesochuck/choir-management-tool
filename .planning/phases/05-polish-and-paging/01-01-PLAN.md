---
phase: 05-polish-and-paging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "pocketbase/pb_migrations/1717500000_phase05_updates.js",
  "src/services/profileService.ts",
  "src/services/venueService.ts",
  "src/services/communicationService.ts",
  "src/lib/rosterImportUtils.ts",
  "src/views/admin/AuditionsView.tsx",
  "src/components/admin/SingerModal.tsx",
  "src/views/admin/RosterView.tsx"
]
autonomous: true
requirements: [
  "rename statuses",
  "pocketbase schema migration",
  "status literal audit"
]
must_haves:
  truths:
    - "Singer statuses are Active, Idle, and Inactive in the UI and database."
    - "Venues and Profiles collection schemas are updated to reflect valid enums."
    - "Communication recipients are indexed by ID for efficient history filtering."
  artifacts:
    - path: "pocketbase/pb_migrations/1717500000_phase05_updates.js"
      provides: "Schema and data migration for statuses and communication indexing"
---

<objective>
Update the platform's status system to use the standardized 'Active', 'Idle', and 'Inactive' labels across all layers (DB, Types, UI) and prepare the communication system for efficient filtering.

Purpose: Align with user-confirmed terminology and ensure data integrity before implementing pagination.
Output: Database migration, updated TypeScript interfaces, and repo-wide literal replacement.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-paging/05-REVIEWS.md
@src/services/profileService.ts
@src/services/communicationService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1.1: Database Schema & Data Migration</name>
  <files>pocketbase/pb_migrations/1717500000_phase05_updates.js</files>
  <action>
    Create a migration to:
    1. Update `profiles` collection `globalStatus` field options to: `['Active', 'Idle', 'Inactive']`.
    2. Backfill `profiles` records:
       - `Active (Current)` -> `Active`
       - `Active (Future)` -> `Idle`
       - Keep `Inactive` as `Inactive`.
    3. Add a `status` select field to `venues` collection with options: `['Active', 'Inactive']`. Default existing records to `Active`.
    4. Add a `recipientIds` JSON field to `messages` collection. 
    5. Backfill `messages.recipientIds` by extracting the `id` from each object in the `recipients` JSON array.
  </action>
  <verify>
    <automated>node pocketbase/pb_migrations/1717500000_phase05_updates.js --check</automated>
  </verify>
  <done>Database schema and data are normalized to new status enums and indexed for communication filtering.</done>
</task>

<task type="auto">
  <name>Task 1.2: Update Service Types and Constants</name>
  <files>src/services/profileService.ts, src/services/venueService.ts, src/services/communicationService.ts, src/lib/rosterImportUtils.ts</files>
  <action>
    1. Update `Profile` interface in `profileService.ts`: `globalStatus: 'Active' | 'Idle' | 'Inactive'`.
    2. Update `Venue` interface in `venueService.ts`: add `status: 'Active' | 'Inactive'`.
    3. Update `MessageRecord` in `communicationService.ts`: add `recipientIds: string[]`.
    4. Update `communicationService.statuses` constant: `['Active', 'Idle', 'Inactive']`.
    5. Update `rosterImportUtils.ts` to alias legacy CSV status names to new values during import.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Service layer reflects the new status architecture and communication indexing.</done>
</task>

<task type="auto">
  <name>Task 1.3: Repo-wide Status Literal Audit & Replacement</name>
  <files>src/views/admin/AuditionsView.tsx, src/components/admin/SingerModal.tsx, src/views/admin/RosterView.tsx, src/components/admin/RosterTable.tsx, src/components/admin/SingerLookupModal.tsx, src/views/ProfileView.tsx, src/lib/seatingSync.ts, src/hooks/useSeatingChart.ts, src/components/admin/RosterImportModal.tsx</files>
  <action>
    Find and replace all string literal occurrences of legacy statuses with new values.
    Specifically:
    - "Active (Current)" -> "Active"
    - "Active (Future)" -> "Idle"
    - "Inactive" -> "Inactive" (Ensure logic remains correct)
    
    Ensure filters, badges, and comparison logic in all listed views are updated.
  </action>
  <verify>
    <automated>grep -r "Active (Current)" src/ | grep -v "node_modules" == 0</automated>
  </verify>
  <done>UI literals and logic are fully migrated to the new status naming convention.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → PB | Schema migration enforces valid enums at the database level |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | profileService | mitigate | Enforce `Active/Idle/Inactive` enums in TypeScript and PB field options |
| T-05-02 | Information Disclosure | communicationService | mitigate | Use the new `recipientIds` field for targeted queries instead of over-fetching history |
</threat_model>

<verification>
Check that `globalStatus` in a Profile record is "Active" instead of "Active (Current)".
Verify that `rosterService` still functions with renamed statuses.
</verification>

<success_criteria>
- All profile records in DB updated.
- No "Active (Current)" literals remain in `src/`.
- `recipientIds` field exists on `messages` records.
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-paging/05-01-SUMMARY.md`
</output>
