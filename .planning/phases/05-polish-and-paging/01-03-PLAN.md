---
phase: 05-polish-and-paging
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [
  "src/views/admin/MusicLibraryView.tsx",
  "src/views/admin/RosterView.tsx",
  "src/views/admin/music-library/MusicLibraryFilters.tsx",
  "src/views/admin/music-library/MusicLibraryTable.tsx",
  "src/components/admin/RosterTable.tsx"
]
autonomous: true
requirements: [
  "client-side pagination",
  "multi-select section filter"
]
must_haves:
  truths:
    - "Music Library and Roster views show paginated results using the shared Pagination component."
    - "Music Library section filter supports selecting multiple sections at once."
  artifacts:
    - path: "src/views/admin/music-library/MusicLibraryFilters.tsx"
      provides: "Multi-select section filtering UI"
---

<objective>
Implement client-side pagination for the Roster and Music Library views using the newly created Pagination component, and upgrade the Music Library section filter to support multi-selection.

Purpose: Improve performance and usability for large choir datasets.
Output: Paginated Roster and Music Library views with enhanced filtering.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-paging/05-02-SUMMARY.md
@src/components/common/Pagination.tsx
@src/views/admin/music-library/MusicLibraryFilters.tsx
@src/views/admin/RosterView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 3.1: Multi-select Section Filter for Music Library</name>
  <files>src/views/admin/music-library/MusicLibraryFilters.tsx, src/views/admin/MusicLibraryView.tsx</files>
  <action>
    1. Update `MusicLibraryFiltersProps` to change `sectionFilter: string` to `sectionFilters: string[]`.
    2. Replace the `<select>` for sections with a multi-select dropdown or a grid of checkboxes (similar to the voice part filter in `RosterView`).
    3. Update `MusicLibraryView.tsx` state: `sectionFilter` -> `sectionFilters: string[]`.
    4. Update `filteredPieces` useMemo to handle `sectionFilters` (any-of logic: show if the piece belongs to any of the selected sections).
    5. Update `buildVisibleMusicLibraryRows` in `src/lib/music/libraryRows.ts` (if needed) to support the array of sections.
  </action>
  <verify>
    <automated>Check that selecting multiple sections shows pieces from all selected sections.</automated>
  </verify>
  <done>Music library supports filtering by multiple sections simultaneously.</done>
</task>

<task type="auto">
  <name>Task 3.2: Integrate Shared Pagination in Music Library</name>
  <files>src/views/admin/music-library/MusicLibraryTable.tsx</files>
  <action>
    1. Import the `Pagination` component created in Plan 02.
    2. Replace the custom pagination footer in `MusicLibraryTable.tsx` with the `Pagination` component.
    3. Ensure `onPageChange` correctly updates the `currentPage` state in `MusicLibraryView`.
    4. Verify "Showing X-Y of Z" label still appears correctly.
  </action>
  <verify>
    <automated>npm run test src/views/admin/music-library/MusicLibraryTable.test.tsx</automated>
  </verify>
  <done>Music Library uses the standard pagination component.</done>
</task>

<task type="auto">
  <name>Task 3.3: Client-Side Pagination for Global Roster</name>
  <files>src/views/admin/RosterView.tsx, src/components/admin/RosterTable.tsx</files>
  <action>
    1. Add `currentPage` and `pageSize` state to `RosterView.tsx` (default 25).
    2. Reset `currentPage` to 1 whenever filters change.
    3. Use `useMemo` to slice `sortedProfiles` based on pagination: `paginatedProfiles = sortedProfiles.slice(...)`.
    4. Update `RosterTable.tsx` to accept `currentPage`, `pageSize`, `totalCount`, and `onPageChange`.
    5. Render the `Pagination` component at the bottom of `RosterTable`.
  </action>
  <verify>
    <automated>Check that the Roster view only shows 25 singers per page and pagination buttons work.</automated>
  </verify>
  <done>Global Roster is paginated, improving render performance for large rosters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Component → UI | Pagination logic remains client-side, no new trust boundaries |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | MusicLibraryFilters | accept | Filtering remains client-side on data already loaded |
</threat_model>

<verification>
Verify that page 1 shows the first 25 items and page 2 shows the next 25.
Verify that clearing filters resets the view to page 1.
</verification>

<output>
After completion, create `.planning/phases/05-polish-and-paging/05-03-SUMMARY.md`
</output>
