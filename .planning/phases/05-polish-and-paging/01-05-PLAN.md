---
phase: 05-polish-and-paging
plan: 05
type: execute
wave: 5
depends_on: ["05-04"]
files_modified: [
  "src/components/admin/SingerModal.tsx",
  "src/views/admin/RosterView.tsx",
  "src/services/profileService.ts",
  "src/components/player/PWAEncouragement.tsx",
  "src/views/PublicPlayerView.tsx"
]
autonomous: true
requirements: [
  "email removal fix",
  "section-less voice parts",
  "PWA encouragement"
]
must_haves:
  truths:
    - "Admin can remove a singer's email address, which deletes their login account while keeping their profile."
    - "Voice parts can be saved without being assigned to a section bucket."
    - "Choir members using the player are prompted to install the app as a PWA."
  artifacts:
    - path: "src/components/player/PWAEncouragement.tsx"
      provides: "UI for encouraging app installation"
---

<objective>
Complete the Phase 05 polish items including a critical fix for email management, relaxing constraints on voice part configuration, and adding PWA "Install" encouragement for choir members.

Purpose: Finalize the platform for production use by addressing known technical debt and improving the mobile experience.
Output: Flexible voice part settings, functional email removal, and PWA installation prompt.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-paging/05-04-SUMMARY.md
@src/components/admin/SingerModal.tsx
@src/services/profileService.ts
@src/views/admin/RosterView.tsx
@src/views/PublicPlayerView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 5.1: Fix Email Removal Logic</name>
  <files>src/services/profileService.ts, src/components/admin/SingerModal.tsx</files>
  <action>
    1. Update `profileService.updateProfile` to detect if the email is explicitly cleared (i.e., `email` is provided as an empty string).
    2. If email is cleared, delete the associated `users` record (if it exists) and set `profiles.user` to `null`.
    3. Ensure `splitProfileInput` in `profileService.ts` correctly preserves the empty string if it's explicitly passed, instead of turning it into `undefined`.
    4. Update `SingerModal.tsx` to ensure clearing the input passes an empty string to the service.
  </action>
  <verify>
    <automated>1. Create a singer with an email. 2. Clear the email and save. 3. Verify the profile remains but has no 'user' relation.</automated>
  </verify>
  <done>Admins can safely remove a singer's login account by clearing their email address.</done>
</task>

<task type="auto">
  <name>Task 5.2: Support Section-less Voice Parts</name>
  <files>src/views/admin/RosterView.tsx, src/lib/voicePartUtils.ts</files>
  <action>
    1. In `RosterView.tsx` (Roster Settings tab), relax the validation that requires a `sectionCode` for every voice part.
    2. Allow saving voice parts where `sectionCode` is empty.
    3. Update `voicePartUtils.ts` (if needed) to handle unassigned voice parts gracefully.
    4. Ensure the UI (filters, seating chart) renders unassigned voice parts in a fallback group (e.g., "Unassigned" or just showing the label).
  </action>
  <verify>
    <automated>1. Add a new voice part with no section assigned. 2. Save settings. 3. Verify it appears in the voice part list in SingerModal.</automated>
  </verify>
  <done>Choirs can configure voice parts that don't belong to a specific section bucket.</done>
</task>

<task type="auto">
  <name>Task 5.3: PWA Encouragement for Player</name>
  <files>src/components/player/PWAEncouragement.tsx, src/views/PublicPlayerView.tsx</files>
  <action>
    1. Create `src/components/player/PWAEncouragement.tsx` which listens for the `beforeinstallprompt` event and shows a non-intrusive "Install this app for offline use" prompt (e.g., a dismissible banner or card).
    2. Include instructions for iOS (Share -> Add to Home Screen) as iOS doesn't support `beforeinstallprompt`.
    3. Integrate the component into `PublicPlayerView.tsx`.
    4. Add a "dismiss" state to `localStorage` so the prompt doesn't annoy returning users.
  </action>
  <verify>
    <automated>Check that the encouragement component appears in the Player view (may require testing in a PWA-capable environment or mocking the event).</automated>
  </verify>
  <done>Choir members are encouraged to install the player as a PWA for better offline experience.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Admin → User DB | Deleting users requires admin privileges (enforced by PB) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Denial of Service | profileService | mitigate | Ensure user deletion in PB is handled gracefully even if the user is already gone |
</threat_model>

<verification>
Confirm email removal deletes the 'users' record.
Confirm voice parts without sections save correctly and appear in dropdowns.
</verification>

<output>
After completion, create `.planning/phases/05-polish-and-paging/05-05-SUMMARY.md`
</output>
