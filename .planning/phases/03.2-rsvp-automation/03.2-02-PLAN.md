---
phase: 03.2-rsvp-automation
plan: 02
type: execute
wave: 2
depends_on: ["03.2-01"]
files_modified: [src/services/communicationService.ts, src/services/rosterService.ts, src/services/profileService.ts]
autonomous: true
requirements: [RSVP-04, RSVP-05]
must_haves:
  truths:
    - "Frontend services can generate signed RSVP tokens"
    - "Dynamic expiration logic for tokens is implemented"
  artifacts:
    - path: "src/services/communicationService.ts"
      provides: "Quick RSVP link generation with HMAC"
---

<objective>
Implement token generation logic in the frontend services, matching the server-side HMAC verification.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-3/SPEC.md
@src/services/communicationService.ts
@src/services/rosterService.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement HMAC Token Generation</name>
  <files>src/services/communicationService.ts</files>
  <action>
    Add `generateQuickRsvpToken(eventId, profileId, choice, expiresAt)` to `communicationService`:
    1. Fetch HMAC secret from `appSettings` (admins have access to this).
    2. Construct payload: `${eventId}|${profileId}|${choice}|${expiresAt}`.
    3. Generate HMAC-SHA256 signature using a library like `crypto-js` or Web Crypto API.
    4. Return `${btoa(payload)}.${btoa(signature)}`.
    
    Add `generateQuickRsvpLinks(event, profile)`:
    - Calculate `expiresAt` as `max(now + 30 days, eventDate + 1 day)`.
    - Generate tokens for 'Yes' and 'No'.
    - Return object with two URLs.
  </action>
  <verify>
    <automated>npm test src/services/communicationService.test.ts</automated>
  </verify>
  <done>Token generation service implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Update Profile and Event Services for new fields</name>
  <files>src/services/profileService.ts, src/services/eventService.ts</files>
  <action>
    Update TypeScript interfaces and service methods to include the new fields:
    - `Profile`: `doNotEmail`, `statusIsManual`, `statusLastChangedAt`, `statusChangeReason`.
    - `Event`: `isOpenForRSVP`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Frontend types and services updated.</done>
</task>

</tasks>

<success_criteria>
Frontend services are capable of generating secure RSVP links and handling the new data schema.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-rsvp-automation/03.2-02-SUMMARY.md`
</output>
