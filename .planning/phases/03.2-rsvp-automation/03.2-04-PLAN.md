---
phase: 03.2-rsvp-automation
plan: 04
type: execute
wave: 4
depends_on: ["03.2-03"]
files_modified: [src/views/QuickRSVPView.tsx, src/App.tsx]
autonomous: true
requirements: [RSVP-08, RSVP-09]
must_haves:
  truths:
    - "Public users can RSVP without logging in"
    - "Public RSVP properly verifies tokens via server-side API"
  artifacts:
    - path: "src/views/QuickRSVPView.tsx"
      provides: "Public interface for token processing"
---

<objective>
Implement the public-facing RSVP view that processes signed tokens.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-3/SPEC.md
@src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create QuickRSVPView</name>
  <files>src/views/QuickRSVPView.tsx</files>
  <action>
    Create a new view component:
    1. Parse `t` (token) from URL search params.
    2. On mount, POST the token to `${PB_URL}/api/quick-rsvp`.
    3. Handle Loading state ("Verifying your RSVP...").
    4. Handle Success state ("Confirmed: [Choice] for [Event Title]").
    5. Handle Error state ("Invalid or expired RSVP link").
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>QuickRSVPView implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Register Public Route</name>
  <files>src/App.tsx</files>
  <action>
    Add a public route `/rsvp` that renders `QuickRSVPView`.
    Ensure this route is accessible without authentication (it should be outside the protected layout).
  </action>
  <verify>
    <automated>grep -q "/rsvp" src/App.tsx</automated>
  </verify>
  <done>Public RSVP route registered.</done>
</task>

</tasks>

<success_criteria>
Users can successfully RSVP to events using tokenized links sent via email.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-rsvp-automation/03.2-04-SUMMARY.md`
</output>
