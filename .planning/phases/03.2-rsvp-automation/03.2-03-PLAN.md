---
phase: 03.2-rsvp-automation
plan: 03
type: execute
wave: 3
depends_on: ["03.2-02"]
files_modified: [src/components/admin/SingerModal.tsx, src/components/admin/RosterTable.tsx, src/components/admin/EventModal.tsx, src/components/admin/MessageComposer.tsx]
autonomous: true
requirements: [RSVP-06, RSVP-07]
must_haves:
  truths:
    - "Admin can toggle manual status and email opt-out"
    - "Admin can see the reason for an automated status change"
    - "Admin can open/close RSVPs for an event"
  artifacts:
    - path: "src/components/admin/SingerModal.tsx"
      provides: "UI for profile automation controls"
---

<objective>
Update the admin interface to support the new RSVP controls and status automation fields.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phase-3/SPEC.md
@src/components/admin/SingerModal.tsx
@src/components/admin/EventModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SingerModal with Automation Controls</name>
  <files>src/components/admin/SingerModal.tsx</files>
  <action>
    Add form fields for:
    - `doNotEmail` (Checkbox/Toggle)
    - `statusIsManual` (Checkbox/Toggle)
    - Read-only display of `statusChangeReason` and `statusLastChangedAt`.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>SingerModal updated.</done>
</task>

<task type="auto">
  <name>Task 2: Update RosterTable and EventModal</name>
  <files>src/components/admin/RosterTable.tsx, src/components/admin/EventModal.tsx</files>
  <action>
    - `RosterTable`: Add a small "No Email" icon next to singers with `doNotEmail` set to true.
    - `EventModal`: Add `isOpenForRSVP` toggle.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>Roster and Event UI updated.</done>
</task>

<task type="auto">
  <name>Task 3: MessageComposer Integration</name>
  <files>src/components/admin/MessageComposer.tsx</files>
  <action>
    Add logic to recognize `{{RSVP_LINKS}}` placeholder.
    When sending, for each recipient:
    1. Generate Yes/No tokens.
    2. Replace placeholder with HTML/Text buttons/links.
  </action>
  <verify>
    <automated>npm run type-check</automated>
  </verify>
  <done>RSVP links can be embedded in messages.</done>
</task>

</tasks>

<success_criteria>
Admin UI allows full management of the new automation and communication features.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-rsvp-automation/03.2-03-SUMMARY.md`
</output>
