# Preserve Set-List Performer Credit Snapshots

Featured Number credits store the roster profile ID together with the public name captured when selected, while guest credits store the captured name alone. This deliberately duplicates roster names so historical and already-published set lists remain stable after a profile is renamed or deleted; admins can intentionally refresh a roster-backed credit by removing and reselecting it.
